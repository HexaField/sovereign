import { test, expect } from '@playwright/test'

test.describe('Recordings', () => {
  test('US-REC-001: start and stop recording in workspace, saved to server', async () => {
    test.skip()
  })
  test('US-REC-002: recording auto-creates meeting and starts transcription', async () => {
    test.skip()
  })
  test('US-REC-003: play back recording with seek support', async () => {
    test.skip()
  })
  test('US-REC-004: recording list shows transcription status badges', async () => {
    test.skip()
  })
})
