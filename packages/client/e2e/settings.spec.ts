import { test, expect } from '@playwright/test'

test.describe('Settings', () => {
  test('US-SET-001: open settings modal via menu', async () => {
    test.skip()
  })
  test('US-SET-002: change setting and it applies immediately', async () => {
    test.skip()
  })
})
