import { test, expect } from '@playwright/test'

test.describe('Navigation', () => {
  test('US-NAV-001: view menu dropdown switches between views', async () => {
    test.skip()
  })
  test('US-NAV-002: Cmd+1 switches to Dashboard', async () => {
    test.skip()
  })
  test('US-NAV-002: Cmd+2 switches to Workspace', async () => {
    test.skip()
  })
  test('US-NAV-002: Cmd+3 switches to Canvas', async () => {
    test.skip()
  })
  test('US-NAV-002: Cmd+4 switches to Planning', async () => {
    test.skip()
  })
  test('US-NAV-002: Cmd+5 switches to System', async () => {
    test.skip()
  })
  test('US-NAV-003: Cmd+Shift+E toggles expanded chat in workspace', async () => {
    test.skip()
  })
  test('US-NAV-004: Cmd+B toggles sidebar visibility', async () => {
    test.skip()
  })
  test('US-NAV-005: connection badge shows connected/disconnected status', async () => {
    test.skip()
  })
})
