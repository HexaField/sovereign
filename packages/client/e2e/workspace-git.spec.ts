import { test, expect } from '@playwright/test'

test.describe('Workspace — Git Panel', () => {
  test('US-WS-014: shows branches, staging area, commit history', async () => {
    test.skip()
  })
  test('US-WS-007: open diff from git panel shows DiffViewerTab', async () => {
    test.skip()
  })
})
