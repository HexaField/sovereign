import { test, expect } from '@playwright/test'

test.describe('Threads', () => {
  test('US-THREAD-001: create a new thread', async () => {
    test.skip()
  })
  test('US-THREAD-002: switch between threads — chat panel updates', async () => {
    test.skip()
  })
  test('US-THREAD-003: threads categorized as entity-bound, user-created, agent-spawned', async () => {
    test.skip()
  })
  test('US-THREAD-004: forward a message to another thread', async () => {
    test.skip()
  })
})
