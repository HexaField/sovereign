import { test, expect } from '@playwright/test'

test.describe('Workspace — Threads Panel', () => {
  test('US-WS-004: shows threads with busy/unread/stuck/error indicators', async () => {
    test.skip()
  })
  test('US-WS-005: clicking thread opens ChatThreadTab with history and live events', async () => {
    test.skip()
  })
})
