import { test, expect } from '@playwright/test'

test.describe('Workspace — Meetings Panel (§8.9.1)', () => {
  test('US-MEET-001: shows meeting list sorted by date with status badges', async () => {
    test.skip()
  })
  test('US-MEET-007: search filters meetings by title, summary, transcript text', async () => {
    test.skip()
  })
  test('US-MEET-006: import button opens ImportDialog for external meeting upload', async () => {
    test.skip()
  })
  test('US-REC-001: record button starts new meeting recording', async () => {
    test.skip()
  })
})
