import { test, expect } from '@playwright/test'

test.describe('Workspace — Files Panel', () => {
  test('US-WS-001: file explorer shows project tree with git status', async () => {
    test.skip()
  })
  test('US-WS-002: clicking file opens FileViewerTab with syntax highlighting and diff markers', async () => {
    test.skip()
  })
})
