import { test, expect } from '@playwright/test'

test.describe('Workspace — Terminal Panel', () => {
  test('US-WS-003: opens embedded PTY terminal scoped to project worktree', async () => {
    test.skip()
  })
})
