import { test, expect } from '@playwright/test'

test.describe('WebSocket / Real-time', () => {
  test('US-WS-RT-001: UI updates in real time via WebSocket', async () => {
    test.skip()
  })
  test('US-WS-RT-002: auto-reconnects after connection drop', async () => {
    test.skip()
  })
  test('US-WS-RT-003: meeting status badges update in real time during transcription', async () => {
    test.skip()
  })
})
