import { describe, it } from 'vitest'

describe('§4.3 MarkdownContent', () => {
  it.todo('MUST render markdown-formatted HTML with Tailwind-styled elements')
  it.todo('MUST render headings with var(--c-text-heading) color')
  it.todo('MUST render code blocks with var(--c-pre-bg) background and copy button')
  it.todo('MUST render inline code with var(--c-code-bg) background')
  it.todo('MUST render blockquotes with left border using var(--c-accent)')
  it.todo('MUST render tables with var(--c-border) borders')
  it.todo('MUST render links with var(--c-accent) color and target="_blank"')
  it.todo('MUST apply syntax highlighting using highlight.js')
  it.todo('MUST sanitize output to prevent XSS')
})
