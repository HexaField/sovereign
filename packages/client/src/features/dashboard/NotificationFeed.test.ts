import { describe, it } from 'vitest'

describe('NotificationFeed', () => {
  describe('§2.5 — Notification Feed', () => {
    it.todo('§2.5 — sources notifications from notifications WS channel')
    it.todo('§2.5 — each notification shows icon, workspace name, summary text, relative timestamp')
    it.todo('§2.5 — clicking notification navigates to correct workspace + entity context')
    it.todo('§2.5 — shows "Mark all read" action')
  })
})
