import { describe, it } from 'vitest'

describe('GlobalChat', () => {
  describe('§2.3 — Global Chat', () => {
    it.todo('§2.3 — renders compact chat panel for _global main thread')
    it.todo('§2.3 — shows last 5 messages truncated with agent status indicator and input area')
    it.todo('§2.3 — sending message uses chat.send WS flow')
    it.todo('§2.3 — clicking header switches to Workspace view → _global → main thread tab')
  })
})
