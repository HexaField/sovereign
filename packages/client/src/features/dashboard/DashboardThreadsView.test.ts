import { describe, it, expect } from 'vitest'

import DashboardThreadsView from './DashboardThreadsView'

describe('DashboardThreadsView', () => {
  it('is a component', () => {
    expect(typeof DashboardThreadsView).toBe('function')
  })
})
