import { describe, it } from 'vitest'

describe('WorkspaceCard', () => {
  describe('§2.2 — Workspace Cards', () => {
    it.todo('§2.2 — renders org name as heading with var(--c-text-heading) and font-weight 600')
    it.todo('§2.2 — shows git summary: projects with uncommitted changes, branches ahead/behind')
    it.todo('§2.2 — shows thread summary: active threads, unread count, error/stuck count')
    it.todo('§2.2 — shows notification count for workspace')
    it.todo('§2.2 — clicking card switches to Workspace view with that org selected')
    it.todo('§2.2 — _global card shows distinct "Global" label with lock icon')
    it.todo('§2.2 — shows colored activity indicator: green (active agents), amber (pending), grey (idle)')
  })

  describe('§2.1 — Card Styling', () => {
    it.todo('§2.1 — card uses var(--c-bg-raised) background with var(--c-border) border and 8px border-radius')
    it.todo('§2.1 — cards have 16px padding and 12px gap')
  })

  describe('§7.2 — Mobile', () => {
    it.todo('§7.2 — cards stack vertically in single column on mobile')
  })
})
