import { describe, it } from 'vitest'

describe('§8.9.4 Dashboard Meeting Widget', () => {
  it.todo('§8.9.4 MUST show recent meetings (last 5) with quick-view summaries')
  it.todo('§8.9.4 MUST show pending transcriptions/summarizations count')
  it.todo('§8.9.4 MUST show total meeting hours this week/month')
  it.todo('§8.9.4 MUST show action items needing attention (open, past due)')
})
