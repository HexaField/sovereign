import { describe, it } from 'vitest'

describe('VoiceWidget', () => {
  describe('§2.4 — Voice Widget', () => {
    it.todo('§2.4 — renders voice interaction widget with large mic button')
    it.todo('§2.4 — tapping starts recording; releasing or tapping again stops and transcribes')
    it.todo('§2.4 — transcription text and agent response appear inline below button')
    it.todo('§2.4 — defaults to _global main thread context')
  })

  describe('§7.2 — Mobile Dashboard', () => {
    it.todo('§7.2 — voice widget is full-width on mobile')
  })
})
