import { describe, it } from 'vitest'

describe('WorkspaceHeader', () => {
  describe('§3.1 — Header Content', () => {
    it.todo('§3.1 — contains workspace selector on left')
    it.todo('§3.1 — contains connection badge')
    it.todo('§3.1 — contains view menu dropdown on right')
  })

  describe('§1.3 — App Root Header', () => {
    it.todo('§1.3 — header MUST NOT contain feature-specific state')
  })

  describe('§8 — Keyboard Shortcuts', () => {
    it.todo('§8 — Cmd+Shift+W opens workspace picker')
    it.todo('§8 — Cmd+P opens command palette / search')
    it.todo('§8 — Cmd+B toggles sidebar')
  })
})
