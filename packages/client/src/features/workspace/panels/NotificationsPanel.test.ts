import { describe, it } from 'vitest'

describe('NotificationsPanel', () => {
  describe('§3.3.5 — Notifications Tab', () => {
    it.todo('§3.3.5 — shows workspace-scoped notifications')
    it.todo('§3.3.5 — subscribes to notifications WS channel')
    it.todo('§3.3.5 — each notification shows icon, summary, relative timestamp, read/unread indicator')
    it.todo('§3.3.5 — clicking navigates to relevant entity tab')
    it.todo('§3.3.5 — shows "Mark all read" action')
  })
})
