import { describe, it } from 'vitest'

describe('FileExplorerPanel', () => {
  describe('§3.3.1 — File Explorer', () => {
    it.todo('§3.3.1 — renders tree view of active project filesystem')
    it.todo('§3.3.1 — fetches file tree from GET /api/files/tree?project=:projectId')
    it.todo('§3.3.1 — subscribes to files WS channel scoped to project')
    it.todo('§3.3.1 — each node shows file icon based on extension, filename, git status indicator')
    it.todo('§3.3.1 — clicking a file opens it in a main content tab')
    it.todo('§3.3.1 — clicking a directory expands/collapses it')
    it.todo('§3.3.1 — right-clicking shows context menu: Open, Open Diff, Copy Path, Reveal in Terminal')
    it.todo('§3.3.1 — header shows active project name with dropdown to switch projects')
    it.todo('§3.3.1 — shows "No project selected" placeholder if no project active')
  })
})
