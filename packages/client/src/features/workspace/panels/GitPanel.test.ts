import { describe, it } from 'vitest'

describe('GitPanel', () => {
  describe('§3.3.2 — Git Tab', () => {
    it.todo('§3.3.2 — shows git status for active project from GET /api/git/status')
    it.todo('§3.3.2 — subscribes to git WS channel scoped to project')
    it.todo('§3.3.2 — shows current branch name, ahead/behind counts')
    it.todo('§3.3.2 — shows list of changed files: staged, unstaged, untracked')
    it.todo('§3.3.2 — clicking changed file opens diff in main content tab')
    it.todo('§3.3.2 — shows active worktrees for project')
  })
})
