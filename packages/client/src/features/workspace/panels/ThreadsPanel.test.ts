import { describe, it } from 'vitest'

describe('ThreadsPanel', () => {
  describe('§3.3.3 — Threads Tab', () => {
    it.todo('§3.3.3 — lists all threads for active workspace')
    it.todo('§3.3.3 — fetches from GET /api/threads?orgId=:orgId')
    it.todo('§3.3.3 — subscribes to threads WS channel for live updates')
    it.todo('§3.3.3 — groups threads: Entity-Bound, User Threads, Agent Threads')
    it.todo('§3.3.3 — each row shows icon, label/entity ref, status indicator, unread count badge')
    it.todo('§3.3.3 — clicking thread switches right-panel chat to that thread')
    it.todo('§3.3.3 — shows "New Thread" button')
    it.todo('§3.3.3 — hidden threads in collapsible "Hidden" section')
  })
})
