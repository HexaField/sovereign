import { describe, it } from 'vitest'

describe('TerminalPanel', () => {
  describe('§3.3.6 — Terminal Tab', () => {
    it.todo('§3.3.6 — renders embedded terminals using existing terminal components')
    it.todo('§3.3.6 — supports multiple terminal instances as sub-tabs')
    it.todo('§3.3.6 — each terminal scoped to project directory within active workspace')
    it.todo('§3.3.6 — uses terminal WS channel for PTY data')
    it.todo('§3.3.6 — "New Terminal" button creates terminal and subscribes to data stream')
    it.todo('§3.3.6 — supports terminal resize on panel resize')
  })

  describe('§8 — Keyboard Shortcuts', () => {
    it.todo('§8 — Cmd+Shift+N creates new terminal')
  })
})
