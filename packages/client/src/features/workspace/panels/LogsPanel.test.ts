import { describe, it } from 'vitest'

describe('LogsPanel', () => {
  describe('§3.3.8 — Logs Tab', () => {
    it.todo('§3.3.8 — shows live event/log stream for active workspace')
    it.todo('§3.3.8 — subscribes to relevant bus events via WS protocol')
    it.todo('§3.3.8 — each entry shows timestamp, level icon/color, module name, message')
    it.todo('§3.3.8 — supports filtering by level and module')
    it.todo('§3.3.8 — auto-scrolls to bottom unless user has scrolled up')
  })
})
