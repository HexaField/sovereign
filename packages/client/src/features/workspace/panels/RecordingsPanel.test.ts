import { describe, it } from 'vitest'

describe('RecordingsPanel', () => {
  describe('§3.3.7 — Recordings Tab', () => {
    it.todo('§3.3.7 — shows list of audio recordings for active workspace')
    it.todo('§3.3.7 — each recording shows timestamp, duration, transcript preview, thread binding')
    it.todo('§3.3.7 — supports playback inline')
    it.todo('§3.3.7 — supports starting a new recording')
    it.todo('§3.3.7 — recordings persisted to {dataDir}/recordings/{orgId}/')
  })
})
