import { describe, it } from 'vitest'

describe('ChatThreadTab', () => {
  describe('§3.5 — Chat Thread Tab', () => {
    it.todo('§3.5 — reuses existing ChatView, InputArea, MessageBubble, MarkdownContent, WorkSection')
    it.todo('§3.5 — subscribes to chat WS channel scoped to active thread key')
    it.todo('§3.5 — supports message forwarding')
  })
})
