import { describe, it } from 'vitest'

describe('FileViewerTab', () => {
  describe('§3.4.1 — File Viewer Tab', () => {
    it.todo('§3.4.1 — displays file content with syntax highlighting')
    it.todo('§3.4.1 — fetches file content from GET /api/files?path=:path&project=:projectId')
    it.todo('§3.4.1 — uses monospace font')
    it.todo('§3.4.1 — shows line numbers')
    it.todo('§3.4.1 — shows git diff markers in gutter for uncommitted changes')
    it.todo('§3.4.1 — tab title shows filename with file-type icon')
    it.todo('§3.4.1 — tab is closable')
    it.todo('§3.4.1 — shows "Read Only" indicator')
  })

  describe('§8 — Keyboard Shortcuts', () => {
    it.todo('§8 — Cmd+W closes active tab')
    it.todo('§8 — Cmd+Shift+T reopens last closed tab')
  })
})
