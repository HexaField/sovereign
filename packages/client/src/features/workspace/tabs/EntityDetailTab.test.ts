import { describe, it } from 'vitest'

describe('EntityDetailTab', () => {
  describe('§3.4.3 — Issue/PR Detail Tab', () => {
    it.todo('§3.4.3 — displays full details of an issue or PR/patch')
    it.todo('§3.4.3 — shows title, status, author, body (markdown rendered), comments, labels, linked threads')
    it.todo('§3.4.3 — shows "Open Thread" button switching right-panel chat to entity-bound thread')
    it.todo('§3.4.3 — tab is closable')
  })
})
