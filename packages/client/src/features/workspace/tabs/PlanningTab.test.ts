import { describe, it } from 'vitest'

describe('PlanningTab', () => {
  describe('§3.4.4 — Planning DAG Tab', () => {
    it.todo('§3.4.4 — displays full planning DAG for active workspace')
    it.todo('§3.4.4 — fetches from GET /api/orgs/:orgId/planning/graph')
    it.todo('§3.4.4 — renders nodes as cards connected by directed edges')
    it.todo('§3.4.4 — critical path is highlighted')
    it.todo('§3.4.4 — blocked nodes show red/amber indicator, ready nodes show green')
    it.todo('§3.4.4 — clicking a node opens its issue/thread')
    it.todo('§3.4.4 — subscribes to planning WS channel for live updates')
  })
})
