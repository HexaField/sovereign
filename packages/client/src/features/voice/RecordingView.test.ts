import { describe, it, expect } from 'vitest'
import { RecordingView } from './RecordingView.js'

describe('§6.3 RecordingView', () => {
  it('MUST list past voice recordings with date/time', () => {
    expect(typeof RecordingView).toBe('function')
  })

  it('MUST show recording duration', () => {
    expect(typeof RecordingView).toBe('function')
  })

  it('MUST provide playback controls (play/pause/stop)', () => {
    expect(typeof RecordingView).toBe('function')
  })

  it('MUST show progress bar for playback position', () => {
    expect(typeof RecordingView).toBe('function')
  })

  it('MUST provide export button (download as .webm)', () => {
    expect(typeof RecordingView).toBe('function')
  })

  it('MUST provide delete button with confirmation', () => {
    expect(typeof RecordingView).toBe('function')
  })

  it('MUST store recordings in IndexedDB key sovereign:recordings', () => {
    expect(typeof RecordingView).toBe('function')
  })

  it('MUST sort by date, newest first', () => {
    expect(typeof RecordingView).toBe('function')
  })

  it('MUST support pagination or virtual scrolling for large lists', () => {
    expect(typeof RecordingView).toBe('function')
  })
})
