import { describe, it } from 'vitest'

describe('§6.3 RecordingView', () => {
  it.todo('MUST list past voice recordings with date/time')
  it.todo('MUST show recording duration')
  it.todo('MUST provide playback controls (play/pause/stop)')
  it.todo('MUST show progress bar for playback position')
  it.todo('MUST provide export button (download as .webm)')
  it.todo('MUST provide delete button with confirmation')
  it.todo('MUST store recordings in IndexedDB key sovereign:recordings')
  it.todo('MUST sort by date, newest first')
  it.todo('MUST support pagination or virtual scrolling for large lists')
})
