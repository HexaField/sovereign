import { describe, it } from 'vitest'

describe('§6.1 VoiceView', () => {
  it.todo('MUST provide a full-screen push-to-talk interface')
  it.todo('MUST have a large central button (minimum 120px on mobile, 80px on desktop)')
  it.todo('MUST show voiceStatusText below the button')
  it.todo('MUST show recordingTimerText when recording is active')
  it.todo('MUST trigger startRecording on press and stopRecording on release')
  it.todo('MUST show static microphone icon in idle state with muted border')
  it.todo('MUST show pulsing microphone with animate-mic-pulse in listening state')
  it.todo('MUST show Spinner in processing state')
  it.todo('MUST show pulsing speaker with animate-speak-pulse in speaking state')
  it.todo('MUST call interruptPlayback and return to idle when tapped during speaking')
  it.todo('MUST use Tailwind utilities with var(--c-*) tokens')
  it.todo('MUST center vertically in available space')
})
