import { describe, it } from 'vitest'

describe('DevicesTab', () => {
  describe('§6.6 — Devices Tab', () => {
    it.todo('§6.6 — shows connected devices and pending pairing requests')
    it.todo('§6.6 — each device shows name, ID, status, last seen')
    it.todo('§6.6 — pending requests show approve/reject buttons')
  })
})
