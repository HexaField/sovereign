import { describe, it } from 'vitest'

describe('SystemView', () => {
  describe('§6.1 — System Tabs', () => {
    it.todo('§6.1 — renders horizontal tab bar with: Architecture, Logs, Health, Config, Devices, Jobs')
    it.todo('§6.1 — only one tab content visible at a time')
  })

  describe('§7.6 — Mobile System', () => {
    it.todo('§7.6 — tabs scroll horizontally if they dont fit on mobile')
    it.todo('§7.6 — config editor stacks fields vertically on mobile')
  })
})
