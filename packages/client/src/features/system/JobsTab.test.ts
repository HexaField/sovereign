import { describe, it } from 'vitest'

describe('JobsTab', () => {
  describe('§6.7 — Jobs Tab', () => {
    it.todo('§6.7 — shows all scheduled jobs')
    it.todo('§6.7 — each job shows name, schedule, last run time + status, next run time')
    it.todo('§6.7 — actions: trigger now, enable/disable, view run history')
  })
})
