import { describe, it } from 'vitest'

describe('ConfigTab', () => {
  describe('§6.5 — Config Tab', () => {
    it.todo('§6.5 — shows current configuration as editable form')
    it.todo('§6.5 — fetches config from GET /api/config')
    it.todo('§6.5 — shows config schema from GET /api/config/schema')
    it.todo('§6.5 — supports editing values inline with type-appropriate inputs')
    it.todo('§6.5 — saving calls PATCH /api/config and shows success/error feedback')
    it.todo('§6.5 — shows change history from GET /api/config/history as collapsible timeline')
    it.todo('§6.5 — changes apply immediately without restart')
  })
})
