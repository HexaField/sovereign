import { describe, it } from 'vitest'

describe('HealthTab', () => {
  describe('§6.4 — Health Tab', () => {
    it.todo('§6.4 — shows system health metrics in card grid layout')
    it.todo('§6.4 — Connection card: WS status, agent backend status, uptime')
    it.todo('§6.4 — Resources card: disk usage, memory usage')
    it.todo('§6.4 — Jobs card: active jobs, last run status, next run time')
    it.todo('§6.4 — Errors card: error count in last hour, last 5 errors with timestamps')
    it.todo('§6.4 — data from GET /api/system/health')
  })
})
