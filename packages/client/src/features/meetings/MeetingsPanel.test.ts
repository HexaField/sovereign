import { describe, it } from 'vitest'

describe('§8.9.1 Meetings Panel', () => {
  it.todo('§8.9.1 MUST display meeting list sorted by date (newest first)')
  it.todo('§8.9.1 MUST show card with title, date, duration, participant count, transcript/summary status badges')
  it.todo('§8.9.1 MUST expand to show summary preview, action items, speaker list')
  it.todo('§8.9.1 MUST open full meeting detail in main content area on click')
  it.todo('§8.9.1 MUST provide search bar to search across titles, summaries, transcripts')
  it.todo('§8.9.1 MUST have Import button for external meeting upload')
  it.todo('§8.9.1 MUST have Record button to start a new meeting recording')
})
