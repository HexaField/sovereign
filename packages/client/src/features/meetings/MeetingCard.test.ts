import { describe, it } from 'vitest'

describe('§8.9.1 Meeting Card', () => {
  it.todo('§8.9.1 MUST display title, date, duration, participant count')
  it.todo('§8.9.1 MUST show transcript and summary status badges')
})
