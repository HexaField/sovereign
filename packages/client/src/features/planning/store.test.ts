import { describe, it } from 'vitest'

describe('Planning Store', () => {
  describe('§5.3 — View State', () => {
    it.todo('§5.3 — exposes viewMode signal defaulting to dag')
    it.todo('§5.3 — viewMode accepts dag, kanban, list, tree')
    it.todo('§5.5 — exposes filters signal as Record<string, string[]>')
    it.todo('§5.5 — exposes searchQuery signal')
  })
})
