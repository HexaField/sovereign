import { describe, it } from 'vitest'

describe('Canvas Store', () => {
  describe('§4.1 — Canvas State', () => {
    it.todo('§4.1 — exposes zoom signal with default value 1')
    it.todo('§4.1 — exposes panX and panY signals with default 0')
    it.todo('§4.4 — exposes selectedNode signal')
  })
})
