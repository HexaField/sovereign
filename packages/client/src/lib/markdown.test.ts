import { describe, it } from 'vitest'

describe('§4.3 Markdown Rendering', () => {
  it.todo('MUST render headings (h1-h6) with var(--c-text-heading) color')
  it.todo('MUST render paragraphs with appropriate line-height and spacing')
  it.todo('MUST render ordered and unordered lists with proper indentation')
  it.todo('MUST render fenced code blocks with var(--c-pre-bg) background and copy button')
  it.todo('MUST apply syntax highlighting using highlight.js')
  it.todo('MUST render inline code with var(--c-code-bg) background')
  it.todo('MUST render blockquotes with left border using var(--c-accent)')
  it.todo('MUST render tables with var(--c-border) borders and alternating row backgrounds')
  it.todo('MUST render links with var(--c-accent) color and target="_blank"')
  it.todo('MUST render strong/emphasis with appropriate font-weight/style')
  it.todo('MUST render horizontal rules with var(--c-border) color')
  it.todo('MUST render images inline with max-width: 100%')
  it.todo('MUST sanitize output to prevent XSS')
})
