import { describe, it } from 'vitest'

describe('Worktrees WS Channel', () => {
  it.todo('registers worktrees channel with correct server message types')
  it.todo('bridges worktree.created bus event to worktree.update WS message')
  it.todo('bridges worktree.removed bus event to worktree.update WS message')
  it.todo('bridges worktree.stale bus event to worktree.stale WS message')
  it.todo('scopes messages by projectId')
})
