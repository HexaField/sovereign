import { describe, it } from 'vitest'

describe('Worktree Git Wrapper', () => {
  it.todo('executes git worktree add with correct branch')
  it.todo('executes git worktree remove')
  it.todo('lists worktrees via git worktree list --porcelain')
  it.todo('detects if branch is merged into target')
  it.todo('deletes branch after confirming merge')
  it.todo('gets last commit date for a worktree')
})
