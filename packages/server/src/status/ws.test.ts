import { describe, it } from 'vitest'

describe('Status WS Channel', () => {
  it.todo('registers status channel with correct server message types')
  it.todo('bridges status.update bus event to status.update WS message')
  it.todo('auto-subscribes new connections to status channel')
  it.todo('scopes status updates appropriately')
})
