import { describe, it } from 'vitest'

describe('Scheduler WS Channel', () => {
  it.todo('registers scheduler channel with correct server message types')
  it.todo('bridges scheduler.job.started bus event to WS message')
  it.todo('bridges scheduler.job.completed bus event to WS message')
  it.todo('bridges scheduler.job.failed bus event to WS message')
  it.todo('only sends to subscribed clients')
})
