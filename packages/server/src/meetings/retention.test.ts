import { describe, it } from 'vitest'

describe('§8.10 Configuration & Retention', () => {
  it.todo('§8.10 MUST register config under recordings.* and voice.*')
  it.todo('§8.10 MUST run daily cleanup scheduler when retentionDays is set')
  it.todo('§8.10 MUST take effect immediately via config.changed bus event')
})
