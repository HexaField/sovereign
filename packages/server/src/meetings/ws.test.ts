import { describe, it } from 'vitest'

describe('§8.7.1 Meetings WS Channel', () => {
  it.todo('§8.7.1 MUST register a meetings WS channel')
  it.todo('§8.7.1 MUST broadcast meeting.created with full Meeting payload')
  it.todo('§8.7.1 MUST broadcast meeting.updated with full Meeting payload on any change')
  it.todo('§8.7.1 MUST broadcast meeting.deleted with { id, orgId }')
  it.todo('§8.7.1 MUST scope by orgId')
})
