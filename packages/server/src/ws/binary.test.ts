import { describe, it } from 'vitest'

describe('Binary Frames', () => {
  it.todo('encodes channel ID as 1-byte prefix')
  it.todo('decodes channel ID from first byte')
  it.todo('preserves payload through encode/decode round-trip')
  it.todo('handles empty payload')
  it.todo('handles max channel ID (255)')
})
