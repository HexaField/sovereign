import { describe, it } from 'vitest'

describe('Event Stream WS', () => {
  it.todo('events channel registered with WS handler')
  it.todo('new events broadcast to subscribers')
  it.todo('scoped subscription only receives matching events')
  it.todo('unscoped subscription receives all events')
  it.todo('history sent on subscribe')
})
