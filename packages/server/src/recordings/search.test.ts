import { describe, it } from 'vitest'

describe('§8.8 Transcript Search', () => {
  it.todo('§8.8 GET /api/orgs/:orgId/recordings/search MUST search transcripts')
  it.todo('§8.8 Transcript search MUST NOT require an external search engine')
})
