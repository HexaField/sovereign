import { describe, it } from 'vitest'

describe('Notifications WS Channel', () => {
  it.todo('registers notifications channel with correct server message types')
  it.todo('bridges notification.created bus event to notification.new WS message')
  it.todo('bridges notification.read bus event to notification.read WS message')
  it.todo('only sends to subscribed clients')
})
