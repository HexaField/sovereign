import { describe, it } from 'vitest'

describe('File Tree Builder', () => {
  it.todo('lists directory contents one level deep')
  it.todo('returns files with type and size')
  it.todo('excludes .git directory by default')
  it.todo('supports custom exclude patterns')
  it.todo('handles symlinks gracefully')
})
