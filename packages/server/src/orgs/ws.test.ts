import { describe, it } from 'vitest'

describe('Orgs WS Channel', () => {
  it.todo('registers orgs channel with correct server message types')
  it.todo('bridges org.created bus event to org.created WS message')
  it.todo('bridges org.updated bus event to org.updated WS message')
  it.todo('bridges org.deleted bus event to org.deleted WS message')
  it.todo('only sends to subscribed clients')
})
