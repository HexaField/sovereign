import { describe, it } from 'vitest'

describe('Monorepo Detection', () => {
  it.todo('detects pnpm-workspace.yaml and lists packages')
  it.todo('detects package.json workspaces field and lists packages')
  it.todo('detects nx.json')
  it.todo('detects turbo.json')
  it.todo('returns null for non-monorepo directory')
})
