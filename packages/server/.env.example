HOST=localhost
PORT=3001

# Sovereign data dir — REQUIRED in any real environment. If unset, falls back to
# <cwd>/.data which silently diverges from any previously-configured dir.
# Recommended: an absolute, version-stable path you own (not inside the repo).
# SOVEREIGN_DATA_DIR=/absolute/path/to/.sovereign-data
